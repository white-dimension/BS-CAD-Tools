# BS-CAD-Tools 本次修改说明

本次修改目标：先不对接 BS-CAD-Standard，优先让 AutoCAD 工具插件更适合日常使用。

## 已完成

1. 主面板重做为更清晰的工具箱结构：
   - 图层工具
   - 自动切换输入法
   - 字体工具
   - 图纸清理

2. 新增主面板按钮：
   - 打开图层管理器 `LY`
   - 修复全图字体 `FIXFONTS`
   - 标准化文字 `TS`
   - 全图物体随层 `SETBYLAYER`
   - 标准环境初始化 `BZ`

3. 增加危险操作确认：
   - `SETBYLAYER` 会修改全图对象属性，执行前确认。
   - `FIXFONTS` 会修改全图文字样式，执行前确认。
   - `TS` 会修改全图文字样式，执行前确认。
   - `BZ` 当前仍是测试入口，执行前提示。
   - 图层删除、图层合并增加二次确认。

4. 增加主面板底部状态栏：
   - 显示最近执行的命令。
   - 显示错误提示。

5. 优化图层管理器入口：
   - `LY` 面板默认宽度从 350 调整为 620，更适合显示图层列表。

6. 路径命名清理：
   - 日志、模板、工具栏顺序保存路径从 `MyAutoCadTools` 改为 `BS-CAD-Tools`。

## 未做

- 未对接 `BS-CAD-Standard`。
- 未读取标准 JSON。
- 未做图层迁移功能。
- 未改 AutoCAD API 版本和 TargetFramework。

## 注意

当前环境没有 `dotnet` 命令，因此无法在沙盒里实际执行 `dotnet build`。
请在你的 Windows 开发环境中运行：

```bash
dotnet build src/BS.CAD.Tools/BS.CAD.Tools.csproj
```

如果编译通过，再用 AutoCAD 的 `NETLOAD` 加载生成的 DLL 测试。

## 2026-06-28 模块化按需显示

本次修改目标：先不对接 BS-CAD-Standard，只让 BS-CAD-Tools 主面板支持按需显示功能模块。

### 新增

- `Models/AppSettings.cs`
  - 保存主面板模块开关配置。
- `Services/SettingsService.cs`
  - 负责读取和保存 `%APPDATA%\BS-CAD-Tools\settings.json`。
  - 第一次启动自动创建默认设置。
  - 默认开启：LayerTools、ImeTools、FontTools。
  - 默认关闭：CleanupTools、StandardTools。
- `Views/ModuleSettingsWindow.xaml`
- `Views/ModuleSettingsWindow.xaml.cs`
  - 新增“模块管理”窗口，可勾选显示/隐藏模块。

### 修改

- `Views/MainPanelView.xaml`
  - 顶部新增“模块设置”按钮。
  - 将功能区域拆成 5 个可隐藏模块：
    - `LayerToolsPanel`
    - `ImeToolsPanel`
    - `FontToolsPanel`
    - `CleanupToolsPanel`
    - `StandardToolsPanel`
  - 将“标准环境初始化 BZ”从“图纸清理”中拆出为单独模块。
- `Views/MainPanelView.xaml.cs`
  - 启动时读取 settings.json 并应用模块显示状态。
  - 保存模块设置后立即刷新主面板，不需要重启 CAD。
- `Commands/PanelCommands.cs`
  - 主面板默认尺寸调整为 340×720，减少按钮拥挤。

### 保留

- 未改变 AutoCAD 命令名称：ShowPanel、LY、FIXFONTS、TS、SETBYLAYER、BZ。
- 未对接 BS-CAD-Standard。
- 未实现图层标准读取。
- 未实现图层迁移。
- 危险命令确认逻辑保留。

### 注意

当前环境没有 dotnet，未能在本机执行 `dotnet build`。下载后请在 Windows 本地运行：

```bash
dotnet build src\BS.CAD.Tools\BS.CAD.Tools.csproj
```


## UI polish update

- 优化主面板整体视觉：更深的专业暗色背景、渐变标题区、卡片化模块、命令徽标。
- 优化按钮样式：统一圆角、hover/pressed 状态、主按钮/危险按钮/普通按钮区分。
- 优化模块层级：当前状态、图层工具、输入法、字体、清理、标准环境视觉权重更清晰。
- 优化模块管理窗口：使用同一套暗色视觉语言，并强化危险模块默认关闭提示。
- 本次只改 XAML 视觉层，不改 AutoCAD 命令名称和业务逻辑。
